{
  "name": "fsrs-chatgpt",
  "private": true,
  "version": "0.1.0",
  "description": "ChatGPT GPT Action integration for the FSRS flashcard service: OpenAPI schema, manifest, GPT instructions, client, and local mock server.",
  "type": "module",
  "scripts": {
    "mock": "node mock-server.mjs",
    "client": "node client.mjs",
    "validate": "node validate-schema.mjs"
  },
  "devDependencies": {
    "js-yaml": "^5.4.1"
  }
}
